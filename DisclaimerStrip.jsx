export default function DisclaimerStrip() {
  return (
    <p className="disclaimer-strip">
      Demo only — a simplified rule-based exercise, not real medical advice.
      In an actual emergency, call your local emergency number.
    </p>
  )
}
